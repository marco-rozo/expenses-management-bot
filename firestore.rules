rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Nega TODAS as leituras e escritas para qualquer documento
    // de qualquer coleção se o acesso vier de um SDK de cliente (web/mobile).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}